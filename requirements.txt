# CalmSpace - Complete Project Dependencies
# This file contains all dependencies for the entire project

# ===========================================
# Backend Dependencies (Node.js)
# ===========================================
# Install with: npm install (in backend/ directory)
# See backend/package.json for exact versions

# ===========================================
# Frontend Dependencies (React)
# ===========================================
# Install with: npm install (in frontend/ directory)
# See frontend/package.json for exact versions

# ===========================================
# ML Service Dependencies (Python)
# ===========================================
# Install with: pip install -r requirements.txt (in ml_service/ directory)

# Core ML Libraries
torch>=2.0.0
transformers>=4.38.0
numpy>=1.24.0
pandas>=2.0.0

# FastAPI and Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# LangChain and AI/ML
langchain>=0.1.13
langchain-community>=0.0.20
langchain-ollama>=0.1.0

# HTTP and API
httpx>=0.25.0
requests>=2.31.0

# Data Processing
scikit-learn>=1.3.0
scipy>=1.11.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
jinja2>=3.1.0

# Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# ===========================================
# System Requirements
# ===========================================
# Node.js >= 16.0.0
# Python >= 3.8.0
# MongoDB Atlas account
# Git

# ===========================================
# Environment Setup
# ===========================================
# 1. Clone the repository
# 2. Set up MongoDB Atlas database
# 3. Configure environment variables
# 4. Install dependencies for each service
# 5. Run all services simultaneously

# ===========================================
# Quick Start Commands
# ===========================================
# Backend: cd backend && npm install && npm run dev
# Frontend: cd frontend && npm install && npm start
# ML Service: cd ml_service && pip install -r requirements.txt && python app.py
